<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Debug builds only (see src/debug/AndroidManifest.xml). Allows plain HTTP to the
      emulator's host-loopback alias so the app can reach a locally-running backend-api
      during development; release builds keep the platform default (HTTPS-only) and
      point at BASE_API_URL over HTTPS instead (see app/build.gradle).
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
